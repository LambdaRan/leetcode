

#include <iostream>
#include <algorithm>
#include <sstream>
#include <functional>
#include <utility>
#include <vector>
#include <string>
#include <map>

//#include <stdio.h>

using namespace std;


int main()
{
    int N;  
    int M;  
    cin >> N;
    cin >> M;
    map<int, vector<int>> m;
    for (int i = 0; i < M; ++i)
    {
        int a, b;
        cin >> a >> b;
    }
    cout << 1 << endl;
    return 0;
}
